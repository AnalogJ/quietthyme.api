{
  "QuietThymeContentBucket": {
    "Type" : "AWS::S3::Bucket",
    "Properties": {
      "BucketName" : "${opt:stage, self:provider.stage}-${self:service}-content"
    }
  },
  "QuietThymeUploadBucket": {
    "Type" : "AWS::S3::Bucket",
    "Properties": {
      "BucketName" : "${opt:stage, self:provider.stage}-${self:service}-upload"
    }
  }
}